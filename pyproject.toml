[project]
name = "blue-battlefield-attendance"
version = "0.1.0"
description = "Fair, auditable Blue Battlefield roster scheduling engine"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
